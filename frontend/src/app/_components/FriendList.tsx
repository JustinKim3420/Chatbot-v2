import { ReactElement } from "react";

const FriendList = (): ReactElement => {
    const data = []
    const loading = false
    const error = null

    return <section className="flex-initial max-w-24 h-full border-2 rounded-md p-4 bg-gray-500 overflow-scroll">
    </section>
}

export default FriendList