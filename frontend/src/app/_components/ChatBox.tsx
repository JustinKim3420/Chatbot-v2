import { ReactElement } from "react";

const ChatBox = (): ReactElement=>{
    return <section className="flex-1 h-full bg-yellow-500">
        hello world chatbox
    </section>
}

export default ChatBox